type Mutation {
  applyForBadge(input: PublicBadgeInput!): PublicBadge
  registerOrganization(input: OrganizationInput!): PendingOrganization
  approveOrganization(input: OrganizationValidation!): Organization
}
